# Add project specific ProGuard rules here.
# By default, the flags in this file are applied to the library's release build.
# Proguard rules from this file are NOT applied to dependent modules.
-keep class com.example.ssoapi.** { *; }
